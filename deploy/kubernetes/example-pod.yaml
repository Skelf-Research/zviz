# Example pod using ZViz runtime
#
# This pod runs in the ZViz sandbox with default security profile.
# Apply with: kubectl apply -f example-pod.yaml

---
apiVersion: v1
kind: Pod
metadata:
  name: zviz-example
  labels:
    app: zviz-example
spec:
  # Use the ZViz runtime class
  runtimeClassName: zviz

  containers:
  - name: app
    image: alpine:latest
    command: ["sh", "-c", "echo 'Running in ZViz sandbox!' && sleep 3600"]
    resources:
      limits:
        memory: "128Mi"
        cpu: "100m"
      requests:
        memory: "64Mi"
        cpu: "50m"

    # Security context (additional restrictions)
    securityContext:
      runAsNonRoot: true
      runAsUser: 1000
      runAsGroup: 1000
      allowPrivilegeEscalation: false
      readOnlyRootFilesystem: true
      capabilities:
        drop:
          - ALL

  # Don't restart on failure (for demo purposes)
  restartPolicy: Never

---
# Example with strict profile
apiVersion: v1
kind: Pod
metadata:
  name: zviz-strict-example
  labels:
    app: zviz-strict-example
spec:
  runtimeClassName: zviz-strict

  containers:
  - name: app
    image: alpine:latest
    command: ["sh", "-c", "echo 'Running in strict ZViz sandbox!' && sleep 3600"]
    resources:
      limits:
        memory: "128Mi"
        cpu: "100m"

  restartPolicy: Never
